import 'package:flutter/material.dart';

class ProfileclickBottomSheet extends StatelessWidget {
  const ProfileclickBottomSheet({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
